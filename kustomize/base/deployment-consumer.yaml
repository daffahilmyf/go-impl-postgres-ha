apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-consumer
  labels:
    app.kubernetes.io/name: app-consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: app-consumer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: app-consumer
    spec:
      containers:
        - name: app-consumer
          image: ghcr.io/daffahilmyf/go-impl-postgres-ha:latest
          imagePullPolicy: IfNotPresent
          args: ["consumer", "--config", "/etc/app/config.yaml"]
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          volumeMounts:
            - name: app-config
              mountPath: /etc/app/config.yaml
              subPath: config.yaml
              readOnly: true
      volumes:
        - name: app-config
          configMap:
            name: app-config
